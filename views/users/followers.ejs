<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
<%- include('../partials/nav') %>
<main class="main-content">
  <div class="container" style="max-width:680px">
    <h1 style="font-size:1.4rem;margin-bottom:0.25rem">Followers of <a href="/u/<%= profileUser.username %>">@<%= profileUser.username %></a></h1>
    <p class="text-muted" style="margin-bottom:1.75rem"><%= users.length %> followers</p>
    <% if (users.length === 0) { %>
      <div class="empty-state"><p>No followers yet.</p></div>
    <% } %>
    <div style="display:flex;flex-direction:column;gap:1rem">
      <% users.forEach(u => { %>
        <a href="/u/<%= u.username %>" class="card" style="display:flex;align-items:center;gap:1rem;text-decoration:none;transition:border-color 0.2s">
          <img src="<%= u.avatarUrl || '/img/default-avatar.png' %>" style="width:48px;height:48px;border-radius:50%;object-fit:cover;flex-shrink:0" alt="<%= u.displayName %>" />
          <div>
            <div style="font-weight:600;color:var(--text)"><%= u.displayName %></div>
            <div style="font-size:0.85rem;color:var(--text-muted)">@<%= u.username %></div>
            <% if (u.bio) { %><div style="font-size:0.85rem;color:var(--text-muted);margin-top:0.25rem"><%= u.bio.substring(0,80) %></div><% } %>
          </div>
        </a>
      <% }) %>
    </div>
  </div>
</main>
<footer class="site-footer"><div class="container"><p>© 2026 <strong>ThinkNote</strong></p></div></footer>
<script src="/js/main.js"></script>
</body>
</html>

