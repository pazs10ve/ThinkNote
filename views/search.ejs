<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Lora:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
<%- include('partials/nav') %>
<main class="main-content">
  <div class="container" style="max-width:860px">

    <!-- Search bar -->
    <form action="/search" method="GET" style="display:flex;gap:0.75rem;margin-bottom:2rem;flex-wrap:wrap">
      <input type="text" name="q" value="<%= q %>" placeholder="Search posts, people, topics…"
             style="flex:1;min-width:200px;background:var(--bg-elevated);border:1px solid var(--border);border-radius:var(--radius-sm);padding:0.7rem 1rem;color:var(--text);font-size:0.95rem;outline:none" />
      <select name="type" style="background:var(--bg-elevated);border:1px solid var(--border);border-radius:var(--radius-sm);padding:0.7rem 1rem;color:var(--text);font-size:0.9rem;outline:none">
        <option value="posts" <%= type==='posts'?'selected':'' %>>Posts</option>
        <option value="users" <%= type==='users'?'selected':'' %>>People</option>
      </select>
      <button type="submit" class="btn btn-primary">Search</button>
    </form>

    <% if (q) { %>
      <p class="text-muted" style="margin-bottom:1.5rem">
        <%= results.length %> result<%= results.length !== 1 ? 's' : '' %> for "<strong><%= q %></strong>"
      </p>
      <% if (results.length === 0) { %>
        <div class="empty-state"><p>No results found. Try a different query.</p></div>
      <% } else if (type === 'posts') { %>
        <div class="post-list">
          <% results.forEach(post => { %><%- include('partials/post-card', { post }) %><% }) %>
        </div>
      <% } else { %>
        <div style="display:flex;flex-direction:column;gap:1rem">
          <% results.forEach(u => { %>
            <a href="/u/<%= u.username %>" class="card" style="display:flex;align-items:center;gap:1rem;text-decoration:none">
              <img src="<%= u.avatarUrl || '/img/default-avatar.png' %>" style="width:48px;height:48px;border-radius:50%;object-fit:cover;flex-shrink:0" alt="<%= u.displayName %>" />
              <div>
                <div style="font-weight:600;color:var(--text)"><%= u.displayName %></div>
                <div style="font-size:0.85rem;color:var(--text-muted)">@<%= u.username %></div>
                <% if (u.bio) { %><div style="font-size:0.85rem;color:var(--text-muted);margin-top:0.2rem"><%= u.bio.substring(0,100) %></div><% } %>
              </div>
            </a>
          <% }) %>
        </div>
      <% } %>
    <% } else { %>
      <div class="empty-state"><p>Enter a query above to search posts or people.</p></div>
    <% } %>
  </div>
</main>
<footer class="site-footer"><div class="container"><p>© 2026 <strong>ThinkNote</strong></p></div></footer>
<script src="/js/main.js"></script>
</body>
</html>
