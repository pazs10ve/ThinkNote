<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/nav') %>
  <main class="main-content">
    <div class="container" style="text-align:center;padding:5rem 1rem">
      <div style="font-size:5rem;margin-bottom:1rem;opacity:0.2"><%= code %></div>
      <h1 style="margin-bottom:0.5rem"><%= code === 404 ? 'Page not found' : 'Something went wrong' %></h1>
      <p style="color:var(--text-muted);margin-bottom:2rem"><%= message %></p>
      <a href="/" class="btn btn-outline">← Go Home</a>
    </div>
  </main>
</body>
</html>
